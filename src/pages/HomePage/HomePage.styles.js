import styled from "@emotion/styled";

export const StyledCrosswordCards = styled.div`
  width: 100%;
  display: grid;
  grid-template-columns: repeat(auto-fill, 20rem);
  gap: 2rem;
  justify-content: center;
`;
